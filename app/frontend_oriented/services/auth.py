# Creating password
# Hashing password
# Verify passwords
from tabnanny import check

# Dictionary (user_id, email, role, hashed_password,


hashed_passw = get_hashed_password(email)


user.get_basic_info(email, user_id)

hashing_passw(email, passw)

update_hashed_password(email, hashed_passw)
    user.update_password(hashed_passw)

verify(token_from_front_end)
    token = user.get_token()
    check(token == token_from_front_end)
